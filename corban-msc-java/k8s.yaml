apiVersion: apps/v1
kind: Deployment
metadata:
  name: corban-msc-java-deployment
  namespace: corban
  labels:
    app: corban-msc-java
spec:
  replicas: 2
  selector:
    matchLabels:
      app: corban-msc-java
  template:
    metadata:
      labels:
        app: corban-msc-java
    spec:
      containers:
        - name: corban-msc-java
          image: corban-msc-java:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: 1024Mi
              cpu: 500m
            limits:
              memory: 2048Mi
              cpu: 1000m
              
---
apiVersion: v1
kind: Service
metadata:
  name: corban-msc-java-service
  namespace: corban
  labels:
    app: corban-msc-java
spec:
  selector:
    app: corban-msc-java
  ports:
    - port: 8080
      targetPort: 8080
      name: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: java-ingress
  namespace: corban
  annotations:
    # 1. Altera o rewrite-target para usar $1
    nginx.ingress.kubernetes.io/rewrite-target: /$1
    # 2. Habilita o uso de RegEx no path (boa prática com $1)
    nginx.ingress.kubernetes.io/use-regex: "true"
    kubernetes.io/ingress.class: "nginx"
spec:
  ingressClassName: nginx
  rules:
    - host: localhost
      http:
        paths:
          - path: /java/(.*)
            pathType: ImplementationSpecific
            backend:
              service:
                name: corban-msc-java-service
                port:
                  number: 8080